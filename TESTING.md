# Testing GlossaryGo

Use this plan for Codex acceptance testing in Raycast. Test observable outcomes against the current product contract;
discover controls from the live UI instead of relying on fixed coordinates, menu positions, or remembered labels.
`README.md` defines expected behavior, `CONTEXT.md` defines terminology, and `package.json` lists current commands,
preferences, runtime requirements, and scripts. A disagreement between those sources and the UI is a finding to resolve,
not a reason to silently change the expected result.

## Continuous integration and manual evidence

`.github/workflows/ci.yml` runs on pull requests and pushes to `main`. Separate macOS checks named `Lint`, `Tests`,
and `Build` install dependencies with `npm ci` and run the corresponding scripts from `package.json`. Node.js is pinned
to 22.22.2, matching the package's minimum runtime requirement. Each check fails on a nonzero
exit; a failed check does not cancel the others. The workflow does not publish the extension. It creates check
results, not branch-protection rules; requiring them for merges is a separate repository setting.

CI uses repository code and synthetic test fixtures only. Never supply a user's Glossary File, local preferences,
private glossary content, or personal clipboard data to CI, logs, caches, or artifacts. The npm cache contains
dependency downloads, not test fixtures or glossary content.

Passing CI is automated evidence, not proof of live Raycast search, reload, clipboard behavior, runtime identity,
or persisted changes after a full restart. Run the affected manual scenarios below and report them separately.
Formatting checks also remain part of local verification; the workflow's checks do not replace the full acceptance plan.

When validating workflow changes, verify its triggers, runtime, locked dependency installation, and all three commands.
In a disposable synthetic checkout, deliberately introduce a lint error, a failing test assertion, and a build error
one at a time; confirm each command exits nonzero, restore the fixture, and rerun the clean checks. When authorized to
publish a test pull request, also confirm each corresponding GitHub check fails and that clean changes recover it.
Report local command evidence separately from GitHub-hosted runs; an unexecuted hosted failure test remains not run.

## Prepare a run

1. Read `AGENTS.md` and the sources above. Record the checkout, branch, commit, uncommitted changes, and requested scope.
   For a full acceptance run, cover every scenario below and each current command. For a feature change, cover its
   affected scenarios plus a search, add, edit, delete, copy, and reload smoke test; identify anything left untested.
2. Use the repository's npm workflow: satisfy the runtime requirement, run `npm ci` when dependencies need installation,
   and start `npm run dev` from the checkout being tested. Confirm which checkout Raycast actually runs using local
   development or installation metadata. Recheck after switching commands or restarting; an installed copy can differ
   from the working checkout. If runtime identity cannot be established, report that limit on UI evidence.
3. Load the available `computer-use:computer-use` skill before interacting with Raycast. Follow its current setup and
   tool instructions; this document intentionally does not duplicate its API or installation paths. If the skill or
   desktop is unavailable, complete independent checks and mark UI cases blocked with a concrete resumption step.
4. Record the original Glossary Location preference and development-mode state without exposing private glossary contents.
   Select a dedicated, disposable existing folder containing a synthetic `glossary.yaml` before opening glossary results
   or exercising writes. Preserve a retained direct `.yaml` preference when testing legacy migration behavior.
   Never copy, log, capture, or back up a personal glossary for testing. Read-only file assertions and fixture preparation
   may use filesystem tools; exercise user-facing search and mutations through Raycast.
5. Build synthetic data with enough terms to exceed the current result limit, deliberately different file and display
   order, overlapping prefixes, accented names, canonically equivalent Unicode queries, multiline definitions, and
   document/entry comments. Include a word found only in a definition. Keep the fixture's starting bytes in memory for
   comparison and restoration. Use separate invalid fixtures for failure cases; never damage a real Glossary File.

For default-file tests, first establish that the effective default path is unused or already a disposable synthetic
fixture. If it contains personal data, leave it intact and report the case blocked until an isolated target is available.

## Observe, act, verify

- Inspect current accessibility state before choosing a control. After each action or short action sequence, fetch fresh
  state before deciding the next step. Resolve element identifiers again from that state.
- Prefer named accessibility controls and keyboard navigation. When accessibility information is incomplete, inspect a
  current screenshot and use its visible controls. Capture only synthetic data; keep evidence local.
- Derive navigation and shortcuts from the current UI. An absent expected action is a finding, not a cue to invoke its
  implementation directly and call that a UI pass.
- Before each case, state the expected outcome. Afterward, compare the visible state and, for mutations, the synthetic
  file's bytes or parsed content. A success toast alone does not establish persistence or correct targeting.
- Verify copy actions with a local comparison to the synthetic expected value. Avoid printing existing clipboard data;
  preserve and restore it in memory when the tools support that, and disclose any restoration limit.
- Keep automated and live UI evidence distinct. If an error cannot be induced reliably in Raycast, cite its automated
  coverage separately and leave its UI status untested or blocked.

## Coverage and fixtures

Before running cases, map every command, preference, README feature, and visible action to the scenario IDs below.
Compare that inventory with the current implementation and test suites. Add missing coverage before calling a run full
acceptance. A row with multiple variants passes only when every variant has evidence; split its report rows as needed.
Record live UI and automated status separately, including explicit blocked or not-run entries.

Prepare these fixture families as needed. Reuse synthetic fixtures across cases, restoring their baseline between
independent cases so previous mutations or recent history cannot hide failures.

| Fixture                 | Required variation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Search and reading      | More shared-prefix terms than fit without scrolling; unsorted file order; mixed case; accent-distinct names; composed/decomposed Unicode queries; a definition-only keyword; a long multiline definition with CommonMark headings, emphasis, lists, blockquotes, an ordinary link, active image syntax, inert `![[...]]` text, raw HTML-like tags, inline code, a fenced ASCII diagram, and Unicode.                                                                                                                                                                                                                        |
| Mutation preservation   | Block and flow sequences; document, sequence, entry, and field comments, including entry-attached comments before blank separator lines and commented entries that Add sorting or first-entry deletion promotes to the front; quoted, literal, and folded scalars; meaningful definition whitespace, including blank lines inside block definitions; adjacent entries with zero, one, and several empty separator lines; unsorted mixed-case and accent-distinct names, including composed/decomposed names; entries whose displayed and file positions differ; same-name siblings with separate and identical definitions. |
| History                 | More distinct entries than the history capacity, including same-name and identical-definition siblings, plus entries that can be renamed, removed, and re-added. Read current limits from the product contract.                                                                                                                                                                                                                                                                                                                                                                                                             |
| Targets                 | Two ordinary custom folders whose `glossary.yaml` files contain distinct sentinel terms; a valid empty glossary; an absent `glossary.yaml` in an existing selected folder; a missing selected folder; existing and missing direct `.yaml` legacy preferences; an isolated default target where safe.                                                                                                                                                                                                                                                                                                                        |
| Validation and failures | One dedicated fixture per failure variant in V2–V4; disposable non-writable, symbolic-link, and multiply hard-linked targets. Use deterministic automated fixtures when Raycast cannot select or reproduce a target reliably.                                                                                                                                                                                                                                                                                                                                                                                               |

## Acceptance scenarios

Use the current README for exact limits, normalization rules, action ordering, and post-save behavior. Choose fresh
synthetic values rather than depending on a fixed fixture inventory. These are planned checks, not claims that they
have already passed.

### Commands, storage, and navigation

| ID  | Exercise                                                                                                                                                                                                                                                                                                                               | Verify                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Launch every manifest command from Raycast root search. Inspect the shared Glossary Location preference and its format-help reference.                                                                                                                                                                                                 | Each command is discoverable with its documented view, inline-argument, or no-view workflow; Quick Add requires both arguments. The optional preference selects a folder, and the format reference agrees with README and the repository schema.                                                                                                                                                                                                                                                                                            |
| C2  | Open every command with an isolated missing default target and with an existing selected folder whose `glossary.yaml` is absent. Select the folder, cancel forms, and submit invalid input; then make a valid first add through each add entry point using a fresh target.                                                             | Selection, read-only access, cancellation, and invalid input create nothing. First valid Add Term or Quick Add Term creates the effective file; only default storage may create its support directory. A selected custom folder must already exist and only its `glossary.yaml` is created. Subsequent commands share that file. Check creation permissions under B2.                                                                                                                                                                       |
| C3  | Select custom folder A, then B, each with a distinct existing `glossary.yaml`; reopen every command after changing preferences. Exercise retained existing and missing direct `.yaml` legacy preferences, then reselect a folder. Clear the preference only with an isolated default available.                                        | Every command uses `<selected folder>/glossary.yaml`. Existing files are loaded and validated without setup overwrite; mutations affect only the effective file and other sentinels stay byte-identical. Existing or missing legacy `.yaml` preferences remain direct paths and are not moved, renamed, or deleted. Reselecting a folder opts into folder behavior; clearing the preference returns to default storage.                                                                                                                     |
| C4  | Add with an existing selected folder whose `glossary.yaml` is absent, then with an existing zero-byte `glossary.yaml`; repeat after the selected folder becomes missing, inaccessible, and unwritable. Also recreate a missing direct legacy `.yaml` whose parent still exists.                                                        | The absent and zero-byte files each initialize only after a valid explicit add. A direct legacy `.yaml` remains the creation target. Missing, inaccessible, and unwritable selected folders fail without creating custom folders, moving legacy files, or falling back to default storage. Recovery succeeds after selecting a valid folder.                                                                                                                                                                                                |
| C5  | Launch standalone Reveal from Raycast root search with selected-folder, retained legacy, and default targets: valid, invalid YAML, empty glossary, blank file, missing file, and missing or inaccessible folders. Also reveal from results, the reader, forms, and recovery views; exercise path/Finder failures and open Preferences. | Finder selects the effective existing file without parsing content, or reveals the nearest existing ancestor. Standalone Reveal keeps missing-target and path/Finder guidance in a dialog after Finder opens. Preferences opens settings; Done dismisses without side effects. A Preferences failure explains manual settings recovery. No file or folder is created; existing bytes stay unchanged. Preferences allows selection of a valid folder. Capture only synthetic locations.                                                      |
| C6  | Inspect actions and shortcut hints with a selected result, no matches, an empty glossary, a missing file, and a load error. Invoke each documented shortcut without opening the action panel.                                                                                                                                          | Selected-result actions follow the README order; shortcut hints match README, are distinct, and invoke the same action as the menu. Return and Command-Return retain the first two copy actions. Add is available in all documented onboarding and no-match states; as the primary action there, it can show the native Return hint while Command-N still invokes it; Edit/Delete require a selection. Load errors expose only file recovery. Loading hides stale actionable results; verify transient states through components if needed. |

### Search, reading, and clipboard

| ID  | Exercise                                                                                                                                                                                                                                                                                                  | Verify                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1  | Search with an empty/whitespace query in a fresh session, a broad prefix, mixed case, surrounding spaces, accent differences, and canonically equivalent Unicode. Try definition-only, middle-of-name, and absent text.                                                                                   | Matching uses term-name prefixes and documented normalization. Include identical, case-equivalent, and canonically equivalent name groups; every sibling remains a separate result with its own definition and distinguishable context. Select a never-copied sibling, reload unchanged source, and verify its highlight/preview survives loading; changed sources select a current valid entry. Accents remain distinct. With no valid history, blank queries show every alphabetical result; no-match state recovers when the query changes. |
| S2  | Search a broad prefix with enough matches to require scrolling. Compare display order with unsorted file order, navigate selection across rows, and reach the final match by scrolling without narrowing the query.                                                                                       | Results sort according to the documented locale-aware policy, retaining source order for equivalent-name ties. Every matching entry is present in the native list with no overflow message or result cap; navigation leaves the complete list and selected row intact.                                                                                                                                                                                                                                                                         |
| S3  | Navigate between short and long definitions containing headings, emphasis, lists, blockquotes, ordinary links, active image syntax, inert `![[...]]` text, raw HTML-like tags, inline code, fenced ASCII diagrams, and meaningful whitespace; scroll the right-pane preview to its final definition line. | Preview belongs to the selected term, shares the reader's CommonMark presentation, reaches the final line, and omits file metadata. Ordinary links remain user-activated links. Active CommonMark image syntax and raw HTML-like tags outside code are inert; `![[...]]` stays literal text; inline code and fenced diagrams retain their exact text, spacing, and line breaks.                                                                                                                                                                |
| S4  | Open a long full definition through both its menu action and shortcut, scroll to its final line, use its actions, and go back.                                                                                                                                                                            | Reader is full-width, titled with the literal selected term, shares the preview's safe CommonMark presentation, and contains the entire definition. Both copy actions and Reveal are available; Copy Definition and Edit retain exact raw source including whitespace; returning retains the query and captured sibling selection when the source is unchanged.                                                                                                                                                                                |
| S5  | Copy both name and definition from both preview and full reader through menu actions and the default Return/Command-Return shortcuts; change selection and repeat.                                                                                                                                        | Clipboard equals the selected original value, including Unicode and multiline content. Success feedback appears and copy keeps the command open. Each successful copy contributes to history.                                                                                                                                                                                                                                                                                                                                                  |
| S6  | Induce a clipboard failure where feasible, then retry successfully.                                                                                                                                                                                                                                       | Failure is reported without recording an unsuccessful copy. Retry works. If failure requires a mock, record automated evidence and leave the live failure variant untested.                                                                                                                                                                                                                                                                                                                                                                    |

### Adding terms and form validation

| ID  | Exercise                                                                                                                                                                                                                         | Verify                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | Open Add through both its menu action and shortcut from a selected result, no-match, empty-glossary, and missing-file view. Use a nonempty query, save, then repeat and cancel.                                                  | Query pre-fills the name; effective path and Reveal are available. Success saves exactly one term and sorts the complete file sequence by the README deterministic policy, returns to Search Term, searches the saved name, and reloads. Cancel leaves bytes and query unchanged.                                                       |
| A2  | Launch standalone Add; save a multiline definition with literal syntax; choose Add Another, save again, then Done.                                                                                                               | Confirmation shows saved values literally. Add Another opens pristine fields without old validation errors and focuses the name. Done closes the command; each entry is saved once, the complete sequence is sorted by the README deterministic policy after each save, and it is searchable on next open or explicit reload.           |
| A3  | Run Quick Add with valid inline arguments, surrounding name whitespace, and literal definition content.                                                                                                                          | No form opens. Name is trimmed, entered definition is preserved, and success follows actual persistence to the effective file. The complete file sequence is sorted by the README deterministic policy; the term becomes searchable on next open or reload.                                                                             |
| A4  | In both add forms and Edit, submit blank/whitespace fields and correct them. Save identical, case-equivalent, and canonically equivalent names, with both different and identical definitions; also try an accent-distinct name. | Invalid input never reaches disk. Forms focus the first invalid field, show field feedback, and clear errors after valid correction. Same-name entries succeed independently, including identical name-and-definition pairs; accents stay distinct for search. Saved names are trimmed while meaningful definition content stays exact. |
| A5  | Run Quick Add with same-name and identical entries, whitespace-only arguments, invalid YAML, and an unwritable target; correct failures and retry.                                                                               | Same-name additions save independently. Failure feedback distinguishes invalid input and save problems; no false success or unintended mutation occurs. Save failures offer file recovery. A corrected retry saves once.                                                                                                                |
| A6  | Enter unsaved add/edit values, cancel or close the command, then reopen.                                                                                                                                                         | No file mutation or saved draft remains. An in-search Add is initialized from the current query; standalone Add is pristine; Edit reads the selected persisted values.                                                                                                                                                                  |
| A7  | Fail a form save, correct the target condition, and retry; attempt repeated submission while saving.                                                                                                                             | Both inputs survive failure, unsorted source bytes stay unchanged, recovery is actionable, and retry saves once with the full sequence sorted. A pending submission does not start a second write. Use B4 for deterministic pending-save coverage.                                                                                      |

### Editing, deleting, and persistence

| ID  | Exercise                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Verify                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | Open Edit through both its menu action and shortcut on a term whose file position differs from its displayed position, using a partial query. Change only its definition, rename it, then make a case- or Unicode-equivalent rename.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Actual selected values pre-fill both fields. Only the captured entry changes, stays at its file position, and retains field comments. Success searches the saved name and reloads. Editing or renaming a selected sibling never changes another, even for identical pairs; equivalent-name renames succeed independently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| M2  | Add from each entry point to block- and flow-sequence preservation fixtures with mixed case, composed/decomposed accents, entry-attached comments, zero/one/several empty lines between entries, and internal block-definition blank lines; include Add sorting and first-entry deletion that promote a commented entry to the front, plus middle deletion in block and flow sequences, with multiple blank lines before, between paragraphs of, and after attached comments, including block empty markers and nonterminal or terminal bare/whitespace-bearing flow-item markers followed by standalone comments; cover multiple source-owned flow markers, terminal markers both with and without a preceding comma, a distinct terminal marker after an existing item comment, zero-gap standalone tails, repeated meaningful saves, unrelated deletion, and marker-owner deletion; separately edit and delete from each restored baseline; also submit an unchanged edit. | Add sorts all entries by the README deterministic policy while moving attached comments/scalar styles and retaining exact decoded definitions. Every saved mutation leaves at least one empty line between adjacent top-level entries, preserves larger existing external entry gaps across block and flow punctuation plus internal definition blank lines, and preserves all comment markers and comment-embedded gaps with their owning entries without loss, duplication, movement, or accumulation. Every distinct flow marker retains its source owner and inline position through repeated saves; deleting an owner removes only that owner's marker. A deleted entry's comments and embedded gaps leave with it while only its external gap transfers to the next entry. Edit keeps the selected position; Delete preserves surviving order. Document, sequence, and surviving-entry comments remain, including standalone tails with no intervening blank line. An unchanged edit preserves exact bytes and file identity without replacement. |
| M3  | Open Delete through both its menu action and shortcut for a selection whose file/display positions differ, then cancel. Repeat with confirmation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Confirmation names the captured term. Exercise each sibling, including identical pairs, from different displayed/file positions. Cancel changes neither file nor query and does not reload. Confirm removes only that term, reports success after save, and reloads without changing the query.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| M4  | Delete with several matches, then delete the last match and finally the last entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Remaining matches are correct; no-match view offers Add. Last deletion leaves a valid empty glossary in the same file, with onboarding actions and surviving document comments.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| M5  | Open Edit or Delete on a same-name sibling, then externally modify, rename, remove, insert before, or reorder entries, or change only comments/BOM before submitting. Also sort via an addition while a selection is captured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Stale mutation is refused without overwriting external changes. Edit retains input and offers explicit reload without changing the query; Delete reloads once and requires retry from a current result. Timing-sensitive replacement conflicts use B3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| M6  | Save additions through every entry point, edit a term, and delete a term. Reopen commands, then perform the full restart check below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | The same verified target contains all saved additions/edits and excludes deletions after both command reopening and full restart. No temporary or alternate file is mistaken for the effective file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### Reload, format validation, and recovery

| ID  | Exercise | Verify |
| --- | --- | --- |
| V1  | Load supported YAML variants: ordinary/flow mappings and sequences, quoted strings, literal/folded multiline values, comments, Unicode, and a valid empty glossary. | All supported forms decode to expected names/definitions. Empty glossary differs from an empty or comment-only file. Compare search loading and mutation validation through B1. |
| V2  | Separately load malformed YAML, empty/comment-only input, multiple documents, duplicate mapping keys, invalid root/entry shapes, extra/missing fields, non-string/empty values, and surrounding whitespace in a source term. | Each invalid source yields an actionable error with a location where supported, without partial or stale results. Distinguish source-file validation from stricter whitespace-only form-input rejection. |
| V3  | Separately load case-/Unicode-equivalent duplicates, anchors, aliases, merge keys, custom tags, and directives. Include an accent-distinct valid control. | Same-name, case-equivalent, and canonically equivalent entries load independently with their own definitions. Unsupported YAML is rejected without exposing raw glossary text. Add/Edit/Delete do not overwrite invalid sources. |
| V4  | Exercise selected folders with missing, unreadable, invalid UTF-8, and oversized effective files; missing, inaccessible, and unwritable selected folders; and retained legacy wrong-extension or missing direct-file targets. Cover the exact size boundary automatically. | Missing effective files use creation onboarding; inaccessible, unwritable, and unsupported targets use actionable recovery. Selected custom folders are never created. Targets are not setup-overwritten, moved, renamed, deleted, or replaced by a default file. Error messages identify the corrective action without leaking source content. |
| V5  | Externally add, edit, and remove synthetic terms while Search Term stays open. Explicitly reload unchanged source, including a BOM-only change, then reload invalid content and repair it. Trigger rapid reloads, change the effective Glossary Location while loading, and close the command during a load. | There is no automatic file watching. Reload retains the query; exact decoded-source reloads reread before reuse and reuse cached parsed terms, while changed source reparses. Failed reload hides stale results. Superseded/path-changed/unmounted loads stop before parsing when feasible, close source handles, never replace current results or show failure UI; post-save refresh reads saved source. Failed, missing, path-switched, and closed-command sessions clear cache, so a later valid reload recovers. |

### Recent Terms and privacy

| ID  | Exercise                                                                                                                                                                                                   | Verify                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | Copy distinct entries, including same-name and identical-definition siblings, using both copy actions in both readers; reuse one; clear the query or enter only spaces. Navigate and type without copying. | Only successful copies enter history, most recent first. Reuse moves only the captured entry to the front without duplicating that entry; passive navigation/typing does not reorder history. The list shows those recent entries first, followed by every remaining alphabetical entry exactly once, without collapsing siblings.                                                                       |
| H2  | With history present, type a prefix matching copied and never-copied terms, copy a typed result, then clear it.                                                                                            | Typed matches remain alphabetical and complete; recording the successful copy does not recompute the unchanged typed match set. Clearing restores the recent-first order followed by every remaining term. If no valid recent names remain, the complete alphabetical list returns.                                                                                                                      |
| H3  | Copy beyond history capacity and reuse an older name before exceeding capacity again.                                                                                                                      | Least-recently-used entries are evicted; reused entries remain recent. Verify retained membership through deterministic history tests, separately from the complete visible list.                                                                                                                                                                                                                        |
| H4  | Reload unchanged source, then after add sorting, a copied definition change, equivalent/distinct rename, sibling removal/reordering, and re-addition of a pruned entry.                                    | Unchanged reload preserves each captured sibling. Changed sources follow unambiguous name/definition matches; singleton equivalent renames and definition changes retain history. Missing/distinctly renamed entries and ambiguous sibling survival prune history conservatively. Re-adding a pruned entry does not restore its history until copied again; every current entry remains visible.         |
| H5  | Fail a reload, recover, load a missing target, switch effective paths, and close/reopen Search Term.                                                                                                       | Failed reload hides history until recovery validates it; missing file clears it. Path changes and command unmount reset history. After a reset or recovery, the complete alphabetical list remains distinct from history membership. A pending successful copy during reload/recovery follows B5.                                                                                                        |
| P1  | Search, read, copy, reload, cancel forms, and reject invalid input against a byte-tracked synthetic fixture. Inspect current persistence/logging/network call sites.                                       | These actions leave unsorted Glossary File bytes unchanged and do not write glossary content, drafts, or history to disk or Raycast storage. Content remains local; clipboard export requires an explicit copy action. Valid mutations use only the effective file and documented temporary save files. Separate code inspection from runtime evidence; absence of visible traffic alone proves nothing. |

## Automated boundary and failure coverage

Run the current test script for a full acceptance run. Use the suites below to locate evidence; a suite's presence does
not prove every listed variant is covered. Inspect assertions and results, record the relevant test names, and report
missing coverage explicitly. Component tests simulate Raycast and never count as live UI evidence.

| ID  | Verify deterministically | Starting points |
| --- | --- | --- |
| B1  | Loader/writer parsing parity; supported YAML and rejection variants in V1–V4; shared Unicode matching and independent same-name entries, including editor-schema acceptance of identical pairs; each loaded name is NFC-normalized once for private identity/search metadata and each typed query once while public terms retain original spelling; deterministic add-only complete sequence order with case/canonical tie-breaking; AST comment/style/definition preservation; mutation-only entry separation for block/flow, punctuation/comment gaps, larger entry gaps, multiline definitions, and idempotence; valid UTF-8 and exact byte-size boundary/overflow for input, multibyte content, and inserted separator bytes. | `src/glossary/glossary-*.test.ts`, `src/glossary/term-matching-policy.test.ts`, `src/glossary/term-name-order.test.ts`, `src/glossary/apply-glossary-change.test.ts`, `src/hooks/search.test.ts` |
| B2  | Default and selected-folder target resolution shared by every command; existing-folder `glossary.yaml` loading; no creation during resolution; exclusive first file creation and restricted permissions; no selected-folder/parent creation; missing, inaccessible, or unwritable folder recovery; direct existing/missing `.yaml` legacy compatibility without move, rename, or deletion; edit/delete never create missing files; ordinary permission preservation; read-only link support and write rejection. | `src/glossary/glossary-target.test.ts`, `src/glossary/glossary-file.test.ts`, `src/glossary/save-glossary-change.test.ts`, `src/quick-add-term-logic.test.ts`, `src/reveal-glossary-file.test.ts` |
| B3  | Exact-byte and identity/metadata conflict checks, including BOM changes and external replacement; same-path save serialization; stale queued edit/delete refusal, selected identical-sibling targeting and refusal after source changes; queue recovery after failure; no-op edit validates selection and avoids replacement while preserving adjacent source bytes exactly, including BOM-prefixed sources; valid mutations retain an existing BOM. | `src/glossary/save-glossary-change.test.ts`, `src/glossary/apply-glossary-change.test.ts` |
| B4  | Repeated form submission/deletion guarding; write, flush, close, and replacement failures preserve existing source; failed first-write and temporary-file cleanup; exclusive-create collisions preserve others' files; cleanup failure does not mask the original error. | `src/components/term-form-logic.test.ts`, `src/components/delete-term-logic.test.ts`, `src/glossary/save-glossary-change.test.ts` |
| B5  | Complete sorted results and recent-first blank-query ordering without collapsing siblings; prepared search data is used for a loaded Glossary, with linear matching through the configured cutoff, indexed ASCII candidates above it, and ICU Unicode matching; typed matches stay cached when recent history changes; history capacity, pruning, and reset; pending successful copies survive loading/failure until recovery; superseded rapid reloads, path changes, and unmounts cancel before stale parsing where feasible without failure UI; exact-source reloads reread before reuse, preserve BOM-sensitive identity, and clear cache on errors, missing paths, path changes, and unmount. | `src/glossary/glossary-source-cache.test.ts`, `src/hooks/search.test.ts`, `src/hooks/search.ts`, `src/hooks/recent-terms.test.ts`, `src/hooks/use-glossary.test.ts`, `src/search-term.test.tsx`, `src/glossary/glossary-decoding.test.ts` |
| B6  | Success-notification failure after persistence does not turn a saved operation into a retryable failed save; refresh failure preserves persisted state and prevents duplicate submission. Unknown failures use safe messages. | `src/components/term-form-logic.test.ts`, `src/components/delete-term-logic.test.ts`, `src/quick-add-term-logic.test.ts` |
| B7  | Shared parser-backed safe CommonMark definition rendering, including code-aware image and raw-HTML neutralization with a zero-hazard token postcondition; action shortcut props, state-specific availability, same-name result context and unique stable row IDs, selected-sibling form/reader/copy wiring, and Delete confirmation/cancellation; exact clipboard values; form focus/error correction; standalone literal confirmation/next-entry state; missing-path Reveal fallback; standalone Reveal target precedence, content-independent reveal, missing ancestors, unchanged files/directories, persistent path/Finder recovery dialogs, dismissal, and Preferences success/failure. | `src/search-term.test.tsx`, `src/utils/prepare-markdown-for-display.test.ts`, `src/utils/prepare-markdown-for-display-micromark.test.ts`, `src/add-term.test.tsx`, `src/utils/render-plain-text-as-markdown.test.ts`, `src/components/standalone-add-term-state.test.ts`, `src/components/reveal-glossary-path.test.ts`, `src/reveal-glossary-file.test.ts` |
| B8  | Run the portable synthetic prefix benchmark. Check current-linear versus indexed matching and ordering parity for duplicate, blank, broad, selective, accent-distinct, NFC/NFD, and no-match queries; record warm/cold latency tails, preparation, lookup, match collection, amortized costs, and runner Node/ICU version. | `bun run benchmark:prefix`, `src/benchmarks/prefix-search-benchmark.test.ts`, `src/benchmarks/prefix-search-benchmark.report.test.ts`, `docs/prefix-search-benchmark.md` |

For changed boundary mechanisms, also exercise a feasible UI failure and recovery path. Do not infer atomic
compare-and-swap or crash durability beyond the README guarantees. Record any leftover synthetic temporary file as a
finding and provide cleanup commands according to `AGENTS.md`.

## Finish and report

1. For code changes or full acceptance, run the current test, lint, formatting, and build scripts from `package.json`.
   For documentation-only changes, check formatting and verify referenced files/scripts and agreement with the product
   contract. Record actual commands, outcomes, and any pre-existing failures.
2. For full restart persistence, stop only the development process associated with this run, quit Raycast completely,
   verify it exited, relaunch, and reopen the command. Verify runtime identity and effective synthetic path again before
   checking saved content. Reopening a command alone is not a full restart check. If switching runtimes prevents the
   check, report it separately from successful command-reopen persistence.
3. Restore synthetic fixtures byte-for-byte, the original preference, and the prior development-mode state. Verify
   restoration without displaying personal glossary contents. Inspect repository status for accidental changes.
   Follow `AGENTS.md`: provide exact cleanup commands for disposable files instead of deleting files yourself. Deleting
   a synthetic term through the extension is a test case; deleting a filesystem file is cleanup.
4. Report **test plan and findings** with checkout/runtime identity, scope, automated results, and one row per scoped
   case/variant: `scenario ID | variant | evidence type | expected | observed | pass/fail/blocked/not run | evidence`.
   Include failures, pending cases, blockers, restoration status, and evidence limits. A full run accounts for every scenario and variant; blocked and not-run cases
   prevent claiming complete verification. Keep run-specific values, screenshots, and findings out of this reusable plan.

## Keep this plan current

Every feature addition, behavior change, or regression fix must update the affected scenario in this file in the same
change. Describe the observable outcome and recovery expectation; add a row only for a distinct user workflow. Reuse
existing rows for new variants and retire obsolete expectations when behavior is intentionally retired. Preserve
scenario IDs for traceability and update the feature inventory and automated coverage pointers when their scope changes.
Keep exact UI labels, numeric limits, storage paths, and script inventories in their existing source of truth rather
than copying them here. Include the plan update and its validation status in the implementation handoff; future coverage is not evidence
that the scenario was executed.
